package com.mbeebe.docket.identity;

import com.mbeebe.docket.DocketTestBase;

/** Identity tests share the app-wide base; nothing identity-specific remains here. */
abstract class IdentityTestBase extends DocketTestBase {
}
